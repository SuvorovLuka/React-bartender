import { createSlice } from "@reduxjs/toolkit";

const AlcoSlice=createSlice({
    name:'alcohol',
    initialState:{
        alcohols:[]
    },
    reducers:{

    }
})

export const {}= AlcoSlice.actions;
export default AlcoSlice.reducer;